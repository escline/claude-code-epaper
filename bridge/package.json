{
  "name": "claude-epaper-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Publishes Claude Code status and usage to MQTT for the e-paper display",
  "main": "bridge.js",
  "scripts": {
    "daemon": "node bridge.js daemon",
    "status": "node bridge.js status",
    "demo": "node bridge.js demo",
    "test": "node test/session-grace.test.js && node test/session-liveness.test.js"
  },
  "dependencies": {
    "mqtt": "^5.10.1"
  }
}
